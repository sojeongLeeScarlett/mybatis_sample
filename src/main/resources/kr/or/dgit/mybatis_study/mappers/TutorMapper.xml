<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.mybatis_study.dao.TutorDao">
	<resultMap type="Tutor" id="TutorResult">
		<id property="tutorId" column="tutor_id"/>
		<result property="name" column="name"/>
		<result property="email" column="email"/>
		<collection property="courses"
		resultMap="kr.or.dgit.mybatis_study.dao.TutorDao.TutorResult"/>
	</resultMap>
	
	<!-- select -->
	<sql id="selectSql">
		select t.tutor_id,t.name as tutor_name,email,c.course_id,c.name,description,start_date,end_date 
		from tutor t 
		left join addresses a on t.addr_id = a.addr_id 
		left outer join courses c on t.tutor_id=c.tutor_id
	</sql>
	<sql id="whereSql">
		<where>
			<if test="tutorId !=null">
				t.tutor_id=#{tutorId}
			</if>
		</where>
	</sql>
	
	<select id="selectTutorByTutorIdWithAPI" parameterType="Tutor" resultMap="TutorResult">
		<include refid="selectSql"/>
		<include refid="whereSql"/>
	</select>
</mapper>